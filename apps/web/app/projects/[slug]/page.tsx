import { TransitionLink } from "../../../components/TransitionLink";
import { notFound } from "next/navigation";
import { projects } from "../../../data/projects";
import { SectionHeading } from "../../../components/SectionHeading";
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}));}
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const project=projects.find(p=>p.slug===slug);if(!project)notFound();const related=projects.filter(p=>p.slug!==project.slug).slice(0,2);return <main className="inner-page project-page"><section className="project-hero section-shell"><div><span className="eyebrow">{project.category.join(" / ")}</span><h1>{project.title}</h1><p className="problem">{project.problem}</p></div><div className="project-hero-art" aria-hidden="true"><div className="art-frame"><span>{project.category[0]}</span></div></div></section><section className="section"><div className="section-shell project-content"><div><SectionHeading eyebrow="01 / OVERVIEW" title="WHY IT EXISTS."/><p>{project.overview}</p></div><div><SectionHeading eyebrow="02 / SOLUTION" title="WHAT I BUILT."/><p>{project.solution}</p></div></div></section><section className="section"><div className="section-shell"><SectionHeading eyebrow="03 / STACK" title="TOOLS IN CONTEXT."/><div className="stack-list">{project.stack.map(s=><span key={s}>{s}</span>)}</div><div className="highlights"><span className="eyebrow">HIGHLIGHTS</span>{project.highlights.map(h=><p key={h}>+ {h}</p>)}</div><div className="evidence-panel"><div><span className="eyebrow">SOURCE-BACKED EVIDENCE</span><h3>{project.sourceLabel ?? "PROJECT RECORD"}</h3><div className="evidence-facts">{project.sourceFacts?.map(f=><p key={f}>+ {f}</p>)}</div></div><div className="evidence-actions">{project.href&&<a className="button" href={project.href} target="_blank" rel="noreferrer">VIEW SOURCE ↗</a>}{project.docsHref&&<a className="button" href={project.docsHref} target="_blank" rel="noreferrer">VIEW DOCUMENTATION ↗</a>}</div></div></div></section><section className="section related"><div className="section-shell"><SectionHeading eyebrow="04 / KEEP EXPLORING" title="RELATED WORK."/><div className="related-grid">{related.map(p=><TransitionLink href={`/projects/${p.slug}`} key={p.slug}><span>{p.category.join(" / ")}</span><h3>{p.title}</h3><b>VIEW ↗</b></TransitionLink>)}</div></div></section></main>}
